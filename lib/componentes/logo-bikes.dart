import 'package:flutter/material.dart';

class LogoBikes extends StatelessWidget {
  const LogoBikes({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}